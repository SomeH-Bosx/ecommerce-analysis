---
description: 每完成一步必须向用户汇报并写入 dev-log.md
alwaysApply: true
---

# 开发步骤汇报与日志

每完成一个可交付步骤后，必须同时做两件事，缺一不可：

1. 在对话里用中文说明：做了什么、改了哪些文件、每个文件分别干什么。
2. 把同样内容追加到仓库根目录 `dev-log.md`。

## 日志格式

```markdown
## [YYYY-MM-DD] 步骤名

- 做了什么：一句话概括本步成果
- 修改文件：
  - `path/to/file`：该文件的职责
```

## 约束

- 只记录本步真实改动，不写计划中的未落地项
- 新增、修改、删除都要列出
- 不要用空泛描述（如“更新了若干文件”）代替具体路径
